import { createStore } from 'vuex'
import ModuleFench from './fence'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    fence: ModuleFench
  }
})
